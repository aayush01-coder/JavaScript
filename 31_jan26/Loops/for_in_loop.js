// for inloop 
// it return the value of key 
let student = {
    name : "Ayush",
    age :15,
    cgpa:8,
    isPass:true,
};
for(let key in student)
{console.log("key =",key , "value =",student[key]);
}